import aboutListener from './about-listener';
import skillsListener from './skills-listener';
import portfolioListener from './portfolio-listener';

export default ({
  aboutListener,
  skillsListener,
  portfolioListener,
});
